method: bayes
metric:
  goal: minimize
  name: Step
parameters:
  Num_episodes_train:
    distribution: int_uniform
    max: 2500
    min: 50
  alpha:
    distribution: uniform
    max: 0.99
    min: 0.5
  epsilon:
    distribution: uniform
    max: 0.99
    min: 0.6
  gamma:
    distribution: uniform
    max: 0.99
    min: 0.5
  randomSeed:
    distribution: int_uniform
    max: 84
    min: 0
program: 
